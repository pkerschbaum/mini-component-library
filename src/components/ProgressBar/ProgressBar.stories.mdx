import { Meta, Story } from '@storybook/addon-docs/blocks';
import ProgressBar from './ProgressBar';

<Meta
  title="ProgressBar"
  component={ProgressBar}
  argTypes={{
    value: {
      control: {
        type: 'number',
        min: 0,
        max: 100,
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
  }}
/>

export const Template = (args) => (
  <div style={{ width: 370 }}>
    <ProgressBar {...args} />
  </div>
);

# ProgressBar

<Story name="Large 50%" args={{ value: 50, size: 'large' }}>
  {Template.bind({})}
</Story>

<Story name="Large 99.8%" args={{ value: 99.8, size: 'large' }}>
  {Template.bind({})}
</Story>

<Story name="Large 100%" args={{ value: 100, size: 'large' }}>
  {Template.bind({})}
</Story>

<Story name="Medium 50%" args={{ value: 50, size: 'medium' }}>
  {Template.bind({})}
</Story>

<Story name="Medium 100%" args={{ value: 100, size: 'medium' }}>
  {Template.bind({})}
</Story>

<Story name="Small 50%" args={{ value: 50, size: 'small' }}>
  {Template.bind({})}
</Story>

<Story name="Small 100%" args={{ value: 100, size: 'small' }}>
  {Template.bind({})}
</Story>
